
<%= form_for (@jobsite) do |f| %>

 <% if @jobsite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobsite.errors.count, "error") %> prohibited this jobsite from being saved:</h2>

      <ul>
        <% @jobsite.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= f.select :customer_id, Customer.all.collect {|p| [ p.company_name, p.id ] } %>

<div class ="field">
  <%= f.label :name %>
  <%= f.text_field :name %>
</div>

<div class ="field">
  <%= f.label :city %>
  <%= f.text_field :city %>
</div>

<div class ="field">
  <%= f.label :state %>
  <%= f.text_field :state %>
</div>

<div class="field">
  <%= f.label :zip %>
  <%= f.text_field :zip %>
</div>

 <div class="actions">
 <%= f.submit "Save",:class => "btn btn-success" %>
 </div>

<% end %>