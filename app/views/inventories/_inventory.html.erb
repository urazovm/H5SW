<%= form_for inventory,:url => inventory_path(inventory),:method => 'PUT',:remote => true do |f| %>
  <%= f.text_field :qty,:style => 'width:76px;height:12px;' %><br/>
  <%= f.submit "update",:style => 'width:48px;height:28px;' %>
  <%= link_to_function 'Cancel',"cancel_update('#{inventory.id}','#{inventory.qty}')" %>
<% end %>
