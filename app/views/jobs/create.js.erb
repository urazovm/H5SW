<% if !@note.new_record? %>
  $('#notes').replaceWith("<%= escape_javascript(render(:partial => 'notes', :locals => {:notes => @notes})) %> ");
  $('#note_description').val(' ')
  $('#note_note_type').val(' ')
<% else %>
<% end %>